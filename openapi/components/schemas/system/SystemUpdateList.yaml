type: object
description: List of tracked software components and their update status.
properties:
  items:
    type: array
    description: Tracked components matching the query. Empty array, never null.
    items:
      $ref: "./SystemUpdate.yaml"
required:
  - items
