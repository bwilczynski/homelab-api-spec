name: type
in: query
required: false
description: |
  Filter updates by component type. Only components of the given type
  are returned. Omit to get all tracked components regardless of type.
schema:
  $ref: "../schemas/system/SystemUpdateType.yaml"
